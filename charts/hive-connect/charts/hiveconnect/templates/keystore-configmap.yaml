apiVersion: v1
kind: ConfigMap
metadata:
  name: "{{ .Chart.Name }}-keystore-volume"
  labels:
    app: "{{ .Chart.Name }}-keystore-volume"
    chart: {{ .Chart.Name }}-{{ .Chart.Version }}
binaryData:
  keystore.p12: |-
    {{ .Files.Get "certs/keystore.p12" | b64enc}}
  truststore.p12: |-
    {{ .Files.Get "certs/truststore.p12" | b64enc}}
