apiVersion: v1
kind: Service
metadata:
  name: {{ include "hive-connect.fullname" . }}
  labels:
    {{- include "hive-connect.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "hive-connect.selectorLabels" . | nindent 4 }}
